import styled from "styled-components";

const Title = styled.span`

background: var(--red);
transform: rotate(-3deg);
color: white;
font-weight: 600;
padding: 5px;
line-height: 1;
font-size: 2rem;
display: inline-block;
//position: absolute; //was not positioning the product title correctly so commented it
bottom: -3px;
left: 100px;

`;

export default Title;